import { LoginForm } from '../../components/LoginForm/LoginForm';

// Login Page:
// a. Create a secure login page featuring a form for entering a username and
// password. (done)
// b. Implement authentication logic to validate the entered credentials, utilizing
// placeholder data for testing purposes. (progress)
// c. Upon successful login, redirect the student to the dashboard page.

export default function LoginPage() {
  return (
    <LoginForm />
  );
}
